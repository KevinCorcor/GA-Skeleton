package enums;

enum Selectors {
    Random, Tournament, RouletteWheel;
 }

 enum Evaluators {
    Length, Ratio;
 }

 enum Reproducers {
    SinglePoint, TwoPoint, UniformPoint;
 }

 enum Mutators {
    RandomBitFlip;
 }